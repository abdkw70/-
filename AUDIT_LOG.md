# سجل التدقيق الفني للمشروع (Project Audit Log)

## 1. تحويل المساعد الذكي إلى Floating Shopping Assistant احترافي وإلغاء الـ Bottom Sheet
- **التاريخ:** 19 سبتمبر 2026
- **المكون المتأثر:** `src/components/Chatbot.tsx`
- **التصنيف:** Floating UI Architecture & Responsive Sizing Overhaul

### المشكلة الأصلية والسبب الجذري للتعديل السابق (Root Cause & Why Bottom Sheet Was Incorrect):
1. **طبيعة الـ Bottom Sheet غير المرغوبة:** التعديل السابق استخدم نمط الورقة السفلية (`fixed bottom-0 inset-x-0 w-full rounded-t-3xl`) مع شريط إمساك سفلي (`drag bar`). هذا النمط كان يمتد عبر الشاشة بأكملها من اليمين إلى اليسار ويلتصق بالأسفل، مما ألغى مفهوم «المساعد العائم المستقل» وحجب مساحة واسعة من واجهة الموقع في الهواتف.
2. **غياب الهوامش الآمنة الجانبية:** كان استخدام `inset-x-0` و`w-full` يجعل النافذة تلتصق بحواف الشاشة بدون أي هامش فاصل.
3. **قصور قيود السحب:** كان السحب غير مقيد بحسابات أبعاد الشاشة الحقيقية ومناطق الأمان، مما قد يسبب خروج النافذة أو تحركها بشكل غير متزن.

### التعديلات والحل المنفذ (Implemented Architectural Solution):
1. **الإلغاء التام لمنطق وفئات الـ Bottom Sheet:**
   - حذف فئات: `fixed bottom-0`, `inset-x-0`, `w-full`, `width: 100%`, `rounded-t-3xl`, `h-[58dvh]`, `max-h-[60dvh]`.
   - حذف شريط الإمساك السفلي الخاص بالـ Bottom Sheet.
2. **تصميم Floating Shopping Assistant حقيقي (مستوحى من فلسفة المساعدات الحديثة مثل Amazon Rufus بهوية Blue Beach):**
   - **الهاتف (Mobile - 320px إلى 430px):**
     - بطاقة عائمة مستقلة بالكامل داخل الشاشة ذات زوايا دائرية مكتملة `rounded-2xl sm:rounded-3xl` وحدود وظلال فخمة `shadow-2xl border border-slate-200/90 dark:border-slate-800`.
     - العرض: `w-[calc(100vw-24px)] max-w-[360px]`، مما يضمن هامشاً آمناً واضحاً (12px على الأقل) من جميع الحواف.
     - الموضع والارتفاع: تطفو فوق شريط التنقل السفلي بهامش آمن `bottom-[76px]` مع مراعاة `env(safe-area-inset-bottom)` و`env(safe-area-inset-left)` و`env(safe-area-inset-right)`.
     - الارتفاع الطبيعي الابتدائي: `h-[460px] max-h-[calc(100dvh-130px)]`، مما يترك أكثر من 45% إلى 50% من شاشة الهاتف العلوية مكشوفة وقابلة للتصفح والتفاعل خلف المساعد.
   - **التابلت (Tablet - 768px إلى 1024px):**
     - بطاقة عائمة تتكيف تلقائياً بحجم مريح للقراءة (عرض 360px إلى 370px، ارتفاع 480px إلى 500px)، وتستقر في إحدى الزوايا العائمة دون أن تتمدد عبر عرض التابلت، تاركة أكثر من 80% من مساحة التابلت مكشوفة للمتجر.
   - **الكمبيوتر (Desktop - 1280px وما فوق):**
     - بطاقة عائمة أنيقة مستقلة بأبعاد ابتدائية مستهدفة: العرض ~360px، الارتفاع ~480px (`sm:w-[360px] sm:h-[480px] sm:max-h-[calc(100vh-120px)]`) تستقر فوق الموقع في الزاوية، مع بقاء الموقع بالكامل مرئياً وقابلاً للتفاعل خلفها.
3. **وضع التكبير والتصغير (Expand / Minimize):**
   - **الوضع الطبيعي (افتراضي عند الفتح):** Floating Medium Chat.
   - **الوضع الموسع (Expand):** يكبر النافذة إلى ~440px عرض و~600px ارتفاع على الكمبيوتر، وإلى كارت عائم أوسع على الهاتف (`max-w-[420px]` بارتفاع موسع)، ولا يتم تفعيله إلا بضغط المستخدم على زر `Maximize2`.
   - **الاستعادة (Minimize):** يعود بضغطة زر إلى الحجم المتوسط الطبيعي.
   - **إعادة الفتح:** دوال الفتح والإغلاق (`handleOpenChat` و`handleCloseChat`) تعيد ضبط الحالة دوماً إلى `isExpanded: false` وموضع الأصل، فلا يفتح المساعد أبداً بحجم ضخم بالخطأ.
4. **نظام السحب المحسوب برمجياً (Desktop Drag with Dynamic Boundaries):**
   - السحب مفعل على **الكمبيوتر فقط** عبر شريط العنوان ومقبض السحب المخصص (`GripHorizontal`).
   - معطل تماماً على الهواتف (`drag={!isMobile}`) لمنع أي تعارض مع إيماءات اللمس والتمرير داخل المحادثة.
   - لا يمكن سحب النافذة عبر نصوص الرسائل أو كروت المنتجات أو أزرار الإغلاق والتكبير (`stopPropagation`).
   - قيود ديناميكية محتسبة بدقة عبر `calculateDragBounds`:
     - حساب `top` لمنع اختفاء الهيدر تحت شريط التنقل العلوي للمتجر.
     - حساب `bottom` لمنع غرق النافذة خارج الحافة السفلية.
     - حساب `left` و`right` بناءً على اتجاه الواجهة (RTL للعربية / LTR للإنجليزية) وعرض الشاشة لمنع خروج النافذة خلف الحواف الجانبية.
     - إعادة حساب الحدود تلقائياً فوراً عند تغيير حجم المتصفح (`resize`) أو تدوير الشاشة (`orientationchange`) أو التبديل بين الوضع العادي والموسع.
5. **السلامة الوظيفية الكاملة:**
   - الحفاظ التام وبنسبة 100% على كافة وظائف المساعد الحالية: نموذج الذكاء الاصطناعي، الاتصال بكتالوج ومخزون المتجر، سجل المحادثات، بطاقات المنتجات التفاعلية للشراء السريع، الاقتراحات السريعة، وعربة التسوق وحساب المستخدم.

---

### نتائج الاختبارات عبر الشاشات المختلفة (Responsive Verification):

| الشاشة ونوع الجهاز | الأبعاد المختبرة | السلوك والنتيجة |
| :--- | :--- | :--- |
| **Mobile الصغير جداً** | 320px (iPhone SE 1st gen) | كارت عائم بعرض 296px مع هوامش 12px من الجانبين، وارتفاع متزن لا يلتصق بالحواف، بدون أي مظهر Bottom Sheet. |
| **Mobile الشائع** | 360px, 375px, 390px (iPhone 12/13/14, Samsung Galaxy S) | كارت عائم بعرض متناسق (336px-360px)، يترك النصف العلوي من واجهة المتجر مكشوفاً، هوامش آمنة واضحة ومستقرة. |
| **Mobile العريض** | 414px, 430px (iPhone Pro Max, Samsung Ultra) | كارت عائم محدد بعرض 360px مع هوامش مريحة حوله، يطفو فوق شريط التنقل السفلي. |
| **Tablet** | 768px (iPad Mini), 820px (iPad Air), 1024px (iPad Pro) | بطاقة عائمة أنيقة في زاوية الشاشة، لا تتمدد بعرض التابلت، وتترك معظم مساحة المتجر ظاهرة وقابلة للتصفح. |
| **Desktop** | 1280px, 1366px, 1440px, 1920px | بطاقة عائمة احترافية 360px × 480px، قابلة للسحب من الهيدر مع قيود حافة صارمة تمنع خروجها عن الشاشة. |
| **الاتجاهات (RTL / LTR)** | العربية (RTL) والإنجليزية (LTR) | استقرار تام للموضع (يسار في العربية، يمين في الإنجليزية) مع احتساب ديناميكي صحيح لحدود السحب. |
| **القيود الملاحظة** | أجهزة فعلية غير محاكاة | التحقق تم عبر بيئة الحاويات والمعاينة المتجاوبة ومحاكاة Viewport ومقاييس CSS Viewport القياسية. |

---

## 2. تدقيق ظهور النصوص والأزرار (UI Visibility & Label Completeness Audit)
- **التاريخ:** 19 سبتمبر 2026
- **المكونات المتأثرة:** 
  - `src/components/gamification/HeaderGamificationWidget.tsx`
  - `src/pages/AccountPage.tsx`
  - `src/pages/AdminDashboard.tsx`
  - `src/components/gamification/EntryChallengeBanner.tsx`
- **التصنيف:** Text Visibility & Accessibility Fix

### التشخيص والإصلاح:
1. **زر تسجيل الدخول:** إزالة فئة الحجب `hidden md:inline` لضمان ظهور كلمة «تسجيل الدخول» / «Login» بجانب الأيقونة على الهواتف وأجهزة الكمبيوتر بلا استثناء.
2. **زر الحساب:** إظهار اسم المستخدم والتسمية التوضيحية بجانب أيقونة المستوى التنافسي.
3. **زر تسجيل الخروج:** إضافة نص صريح «تسجيل الخروج» / «Logout» بجانب أيقونة الخروج في صفحة الحساب.
4. **لوحة التحكم:** إظهار نص زر «المتجر» في الهيدر المحمول للوحة الإدارة.
5. **شريط التحدي الترويجي:** تعديل النص الفرعي ليظهر على الهواتف بشكل منسق مع حمايته من التجاوز (`line-clamp-1 sm:line-clamp-none`).
